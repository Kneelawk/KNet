Changes:

* Added `SimpleChildChannelContext` for if no extra payload needs to be sent to describe the relationship between a
  parent context and a child context.
* Added debug properties `com.kneelawk.knet.debug` for printing when messages are sent and received
  and `com.kneelawk.knet.debug.stackTraces` for printing stack traces as well.
